# 研迹 MedResearch

医学系统综述文献筛选与证据管理的静态 PWA。所有项目数据默认保存在用户当前浏览器的 `localStorage`，不会上传到服务器。

支持：CSV、RIS、NBIB、BibTeX 导入；DOI/题名去重；文献库搜索与状态修改；项目纳排标准；题名摘要与全文筛选及撤销；PRISMA 数量统计与打印；证据矩阵和质量评价；CSV 导出；JSON 备份和恢复。

本仓库从 `main` 分支根目录发布到 GitHub Pages；后续更新文件并推送后会自动重新发布。
